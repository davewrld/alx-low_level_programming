#include "main.h"

/**
 * _abs - Function that prints the absolute value of an integer
 * @n: integer to be computed
 *
 * Return: the absolute value of the intenger.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
